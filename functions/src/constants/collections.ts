export enum COLLECTION_NAMES {
  BIN_SELECTION_POSTCODE_CONFIG = "bin-selection-postcode-config",
  ANALYTICS = "analytics",
  PRODUCTS = "products",
  REPORTS = "reports",
  USER_BIN_SELECTIONS = "user-bin-selections"
}